#!/bin/bash
# Mark a phase task as done (set aistate:done, then complete it).
# Usage: phase-done <task-id>
set -euo pipefail
source "$(dirname "${BASH_SOURCE[0]}")/guard.sh"
require_context worktree

if [ $# -lt 1 ]; then
    echo "Usage: phase-done <task-id>"
    exit 2
fi

TASK_ID="$1"

task "$TASK_ID" modify aistate:done
task "$TASK_ID" done
# shellcheck disable=SC2046  # word splitting is intended: three key=value args
ai_heartbeat phase-done $(ai_task_fields "$TASK_ID")
echo "OK: Task $TASK_ID marked done."
