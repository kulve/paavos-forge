#!/bin/bash
# Project-local Taskwarrior wrapper. Always use this instead of bare `task`.
set -euo pipefail
# No require_context: the wrapper is valid in both the main tree and worktrees.
# shellcheck source=guard.sh
source "$(dirname "${BASH_SOURCE[0]}")/guard.sh"
exec task "$@"
