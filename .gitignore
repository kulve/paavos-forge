# Editor and OS
.DS_Store
*.swp
*~

# Cursor IDE local config at repo root only (not deployable templates)
/.cursor/

# Local Taskwarrior / worktree state (never commit)
.task/
.worktrees/

# Dependencies of the model catalog query (installed on demand)
scripts/models/node_modules/
